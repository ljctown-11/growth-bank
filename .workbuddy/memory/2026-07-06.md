# 2026-07-06 工作日志

## 新增规则：GitHub/Gitee 推送权限控制

用户明确要求：
- **未经用户明确指示，禁止向 GitHub 和 Gitee 推送代码。**
- 当前处于本地修改和测试阶段，上传时机由用户决定。
- 用户明确说"上传"或"推送"后，才能执行 `git push`。
- 同时遵守"执行吧"设定：未经用户确认，不自行推进执行。

## BugFix-Code-Review（5个Bug）
- 删除 `checked.clear()`（变量未定义，导致午夜日期切换崩溃）
- SW 注册加 `file://` 防护（避免 Edge CORS 错误）
- 定义 `dismissAutofill()` 函数（多弹窗调用未定义函数）
- 新建宝宝后自动 `switchChildFromParentCenter(id)`
- manifest 链接在 `file://` 下动态禁用

## BugFix-Comprehensive（9项修复）
- **补卡全取消→积分返还**：居中显示退款消息（`showEncourageMsg`），强制清除旧弹窗
- **childName为空→阻止勾选**：居中提示 + 立即还原 checkbox 状态
- **分数不足→居中提示**：补卡/兑换/未选择奖励的 toast→`showEncourageMsg`
- **兑换按钮文案**：源码已正确（用户看到旧浏览器缓存）
- **复盘按周保存**：旧 `date` → 新 `weekStart/weekEnd`，自动迁移，数据合并
- **复盘所有表可编辑**：所有记录显示编辑按钮，编辑需 `showPasswordModal` 密码验证
- **切换日期→作品关联刷新**：`setSelDate` + `renderAll` 均触发 `renderWorksDropdown`
- **宝宝信息首次创建需密码**：`hasParentPassword()` 已设置→密码验证；未设置→允许（避免死锁）
- **媒体文件大小限制**：图片≤5MB，视频≤50MB，音频≤50MB；超标→居中提示+清除选择
- **媒体存储评估**：IndexedDB，2个月约1.4GB，完全撑得住

## 修改文件
- `features/render.js`：居中提示、补卡退款、childName校验、复盘编辑
- `main.js`：复盘系统重构、宝宝密码、媒体限制、数据迁移 IIFE
- `core/state.js`：setSelDate 触发 renderWorksDropdown
- `features/parent-center.js`：新建宝宝自动切换

## 修复迭代（2026-07-06 17:00）
- `renderCalendar()` 加 `isNaN()` 防御：`calYear`/`calMonth`/`lastDate` 异常时回退默认值，防止日历只显示1格
- `toggleTask()`：名字为空时回退 checkbox 勾选状态（先提示"请先设置宝贝信息"）
- `main.js` babyName 弹窗：保存宝贝信息前增加 `showPasswordModal()` 密码确认
- 部署到 CloudStudio 验证

## 宝贝名字为空→任务灰色不可勾选（2026-07-06 18:39）
- **文件**: `features/render.js`, `index.html`, `main.js`, `sw.js`
- **修改**: `renderTasks()` 中两处 checkbox 渲染（补卡+正常模式）添加 `nameEmpty` 判断：checkbox 加 `disabled`，label 加 `name-empty-disabled`
- **CSS**: `.task-row.name-empty-disabled{opacity:0.45;pointer-events:none;filter:grayscale(80%)}`
- **版本**: SW_VERSION 和 CACHE_NAME 升级到 v3.2
- **部署**: https://9d7cbb9a19a94286aa699e835cd638f8.app.codebuddy.work