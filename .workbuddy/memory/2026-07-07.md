# 2026-07-07 工作日志

## 修复：部署不生效（index.html 未切到 ES module）

**根因**：`index.html` 第 875 行仍是 `<script src="bundle.js?v=3.0.4">`，从未切到 `main.js` 模块入口。导致 `render.js`/`main.js` 等所有改动均未被加载，SW 也只缓存了 `bundle.js`。

**修复**：
- `index.html` → `<script type="module" src="main.js?v=3.3">`
- `sw.js` → `CACHE_NAME='summer-growth-bank-v3.3'`，`ASSETS` 补全所有模块文件（core/* + features/*）
- `main.js` → `SW_VERSION='summer-growth-bank-v3.3'`，register URL `sw.js?v=3.3`

**关键点**：之前摘要里"已切到 module"实际未落到文件，是旧 bundle.js 一直在生效。

**用户清除旧缓存步骤**（浏览器侧）：
1. F12 → Application → Service Workers → Unregister
2. Application → Storage → Clear site data（清 localStorage + Cache）
3. 或开无痕窗口直接访问

**部署**：CloudStudio https://9d7cbb9a19a94286aa699e835cd638f8.app.codebuddy.work 已验证线上 HTML 为 `main.js?v=3.3`

## 修复：6 个 Bug（缺陷A + 方案B存储重构 + 问题3 + 版本号）

**用户需求**：1) 没设密码应提示"请设置家长密码"(非随机鼓励词)；2) 设密码后保存失败；3) 家长中心新增宝宝后信息立即同步到"设置宝贝信息"；4) 新增宝宝消失/点兑换日历掉线；5) 配色主题失效；6) 该弹密码验证处误弹随机鼓励词。版本号从 V3.1.01 起。

**根因（2 个核心缺陷）**：
- 缺陷A：`showEncourageMsg(customMsg)` 忽略传入文字，强制随机鼓励词 → 问题1、6
- 缺陷B：孩子元信息（children/childName/theme）双写存储模型 + `loadData` 按名字匹配误杀 childName → 问题2/3/4/5

**方案**：缺陷A 改 1 行；问题3 选方案B 存储重构（元信息只在 `main.children` 单一真相源，`_child_<id>` 快照只存打卡数据）。

**改动文件**：
- `features/render.js`：L481 `const msg = customMsg || _ENCOURAGES[...]`
- `core/data.js`：`loadData` 改为按 `main.children[activeChildId]` 派生元信息（删名字匹配清空）；`saveData` 解构剥离元信息
- `features/parent-center.js`：`doAdd` 成功改 `switchChildFromParentCenter(id)`（问题3）；`switchChildFromParentCenter` 整体重写按 id 派生元信息 + `saveData()`
- `main.js`：init 块按 id 派生元信息；`SW_VERSION`/`register` → v3.1.01
- `sw.js`：`CACHE_NAME` → v3.1.01
- `index.html`：脚本 `main.js?v=3.3` → `?v=3.1.01`（击穿浏览器脚本缓存，关键）

**验证**：node ESM 语法 5/5 通过；QA 判定 PASS（路由 NoOne）。未推送 GitHub/Gitee（用户未明确指示）。

**部署**：CloudStudio 同链接，已验证线上 HTML 为 `main.js?v=3.1.01`。

## 整体查 BUG + 修复（P1-P9，版本号 v3.1.02→v3.1.03）

**背景**：上一轮发版实际版本号为 v3.1.02（非当初说的 v3.1.01，中间有一次未说明的发版）。用户要求"整体查BUG"→ 全量静态审查。

**审查发现**（工程师审查，主理人已 grep 验证）：
- **P1【严重】**：`features/parent-center.js` 调用 6 个未 import 的函数（applyTheme/renderAll/clearDailyReminderFlag/requestNotificationPermission/scheduleDailyReminder/checkGrowthReportDay），这些函数定义在 `main.js` 但未 export，ES 模块隔离 → `switchChildFromParentCenter` 抛 ReferenceError → **多孩子切换/新增/删除功能实际崩溃**。这是比之前6个问题更致命的阻断级 Bug。
- **P2**：`parent-center.js` 调用未定义 `setStateFromSaved` → 改 `hydrateStateFrom(loadData())`
- **P3**：`getChildren()` JSON.parse 无 try-catch → 包 try-catch
- **P4**：`switchChildFromParentCenter` default 分支 `hydrateStateFrom(savedMain)` 清空打卡数据 → 改 `loadData()`
- **P5-P9【轻微】**：setData undefined 覆盖、SW 缓存串不匹配、未用 import 冗余、重复 ENCOURAGES 定义等

**6 个旧修复点回归结论**：✅ 无回归（缺陷A/方案B/问题3-doAdd/问题4-reload还原/问题5-theme派生 均正确落地）。

**修复**（用户说"直接执行吧"后，走 BugFix 路径，建 team software-bugfix-p1）：
- P1：`main.js` 5 函数加 `export`；`parent-center.js` 补 2 行 import（renderAll 来自 render.js，其余5个来自 main.js）。循环依赖安全（函数声明提升+仅事件回调内调用）。
- P2-P4：按上改
- P5：`core/state.js` setData 加 `if(merged[k]!==undefined)`
- P6：`sw.js` ASSETS 的 .js 引用加 `?v=3.1.03`
- P7：清理 6 个文件未用 import（先 grep 确认无残留）
- P8：删 main.js 冗余 ENCOURAGES import
- 版本号 4 处统一 v3.1.03（main.js SW_VERSION/register、sw.js CACHE_NAME、index.html script）

**验证**：8 个 JS 文件 node --check 全过；QA 判定 PASS（路由 NoOne）。主理人已 grep 核验 P1 import/export 配对、版本号 4 处一致。

**部署**：CloudStudio 同链接，已验证线上 HTML 为 `main.js?v=3.1.03`。

**重要经验**：
1. 模块化拆分后，跨文件调用必须 import/export 配对——漏 import 在模块加载下必崩（file:// 或 bundle 拼接的旧方式掩盖了此问题）。
2. 用户铁律：未说"执行吧"只分析不动文件；未明确指示不推送。
3. 当前环境无 TeamCreate 自动可用，需先 TeamCreate 才能 Agent 派 teammate（否则报"No active team found"）。

## 修复：设置宝贝信息保存失败（闭包作用域，版本号 v3.1.04）

**Bug**：从「设置宝贝信息」页保存宝宝信息即失败（弹"操作失败，请重试"），宝宝进不了家长中心；「家长中心」入口正常。

**根因**：`main.js` 中 `save()`（~181）与 `doSaveAfterPassword()`（~128）为同级兄弟函数，都定义在 #babyName 点击处理器内。`save()` 内 `var name/gender/theme`（~182-186）仅其自身作用域；`doSaveAfterPassword` 在 ~131 引用这三者 → `name` 偷成全局 window.name（空串），`gender/theme` 直接 `ReferenceError`。报错发生在 localStorage 写入块（~145-170）之前，密码模块 catch 后弹"操作失败"，表现为"该页所有操作都失败"。

**修复**（最小 A+B，用户选推荐方案）：
- A：`function doSaveAfterPassword(){` → `function doSaveAfterPassword(name, gender, theme){` (main.js:128)
- B：回调 `doSaveAfterPassword()` → `doSaveAfterPassword(name, gender, theme)` (main.js:197)
- 版本号 v3.1.03→v3.1.04，5 处一致（main.js SW_VERSION/register、sw.js CACHE_NAME/ASSETS、index.html script）

**验证**：QA 用 Node 逻辑仿真复现修复前 ReferenceError + 验证修复后值正确；13 模块 node --check 全过。路由 NoOne，PASS。

**QA 范围误报澄清**：`git diff HEAD` 显示 194 行脏改动，系之前 v3.1.x 多轮重构（家长中心重写/bundle→ESM/存储模型/P1-P9）累积未提交，非本轮引入；本轮仅动 main.js A+B + 版本号。parent-center.js 本轮未碰，无回归风险。

**未推送**（用户铁律）：所有改动留本地工作树，未提交未推 GitHub/Gitee。

**部署**：CloudStudio 预览已重部署（同沙箱 9d7cbb9a19a94286aa699e835cd638f8.app.codebuddy.work），线上 index.html 为 `main.js?v=3.1.04`。属沙箱预览，非 GitHub/Gitee 推送。

## 功能：补卡返还逻辑修复 + 日历已打卡高亮（标准 SOP，版本号 v3.1.05）

**用户需求**（两条）：
1. 补卡第3次扣10、第4次扣20；取消补卡任务时检查当天还有无勾选——有则不返还，全空才返还扣分并回退当月次数。
2. 已打卡日期日历高亮区分，当天分值也高亮。

**流程**：标准 SOP 全流程（用户选）。建 team software-makeup-return → 产品经理 PRD → 架构师设计 → 工程师编码 → QA 验证。

**范围冻结（拍板）**：返还判定=「全空才返还+回退次数」(makeupUsed 可恢复、回退后允许再补)；A-4 轻量 toast 反馈做；P2「补」角标不做；不改 makeup.js 扣分规则本身。

**改动文件**：
- `features/render.js`：~:418 扣分记录加 `makeupDate: STATE.selDate`（修日期匹配 bug：旧用 `r.date` 斜杠 vs `selDate` 横杠比对接不上）；~:441-460 取消分支重写 `stillHas=Object.values(day.tasks).some(t=>t&&t.done)`，仅 `!stillHas` 才删 `level:"补卡"&&r.makeupDate===selDate` 记录+清 `makeupVerifiedDates`+`makeupUsed[mk]-1`+toast「已返还 X 分，补卡次数 -1」；~:179 日历格 `score>0` 加 `has-pts` 类。
- `index.html`：`:root`+5 主题(sakura/ocean/forest/sunset/starry)各定义 `--cell-checked-bg`/`--pts-chip-bg`/`--pts-chip-fg`；升级 `.cal-cell.has-pts` 整格高亮+`.day-pts` 色块（基于 `--leaf` 变量，5 主题均定义 `--leaf`/`--leaf-rgb`，无失效）。
- 版本号 v3.1.04→v3.1.05，5 处一致。

**验证**：QA 逻辑仿真 11/11 通过（场景1 还有勾选保留/场景2 全空返还/场景3 旧斜杠脏数据按 makeupDate 精确匹配）；13 模块语法 OK；5 主题变量齐全。路由 NoOne，PASS。

**未推送**（铁律）：留本地，未提交未推 GitHub/Gitee。

**部署**：用户要求后已重部署 CloudStudio（同沙箱 9d7cbb9a19a94286aa699e835cd638f8.app.codebuddy.work），已命令行 curl 核实线上 `index.html` 加载 `main.js?v=3.1.05`（此前预览仍是旧的 v3.1.04，故用户看到版本号没变）。属沙箱预览，非 GitHub/Gitee 推送。

## 新需求批次：8 点混合（分析完成，待"执行吧"）

用户提 8 点（分析已做，未改文件）。用户拍板：**一次性全做（标准 SOP 全流程）**，上传大小规格 **图片 ≤10MB / 视频 ≤100MB**。目标版本号拟升 v3.1.06。

1. 设置宝贝信息保存后弹窗不自动关 + 强制从「学习力」分类开始 → main.js doSaveAfterPassword 把 ov.remove() 提前/包 try，收尾 switchToCategory('学习力')。
2. 补卡余额不足提示改居中高亮（reuse password.js 居中样式，绿/橙调）→ render.js:392-396。
3. 日历日期数字高亮（不止分数）：CSS `.cal-cell.has-pts .day-num` 加主题色加粗/角标 → index.html。
4. 兑换页"继续存分"→"还差 X 分"：render.js:708 按 cost-total 渲染。
5. 所有密码验证成功无提示 → password.js:100 成功分支加居中"✓ 验证通过"绿闪。
6. 复盘：每周一张（按周键 upsert，非每次新表）+ 每条都可编辑（去掉 isLatest 限制，复用 revEditIdx）→ main.js saveReview + render.js renderReviewTimeline；旧记录按 date 推导周键兼容。
7. 成长作品关联"具体某天"已完成任务：作品表单加显式日期选择，任务下拉按所选日期已完成任务填充 → main.js renderWorksDropdown/saveWork。
8. 存入档案双份记录（写入前去重 artwork.id + 确认 saveWork 只绑一次）+ 上传大小上限(图10MB/视频100MB)超限拒绝 + index.html 上传框 `<small>` 写明限制并加 accept。

**待执行确认**：等用户说"执行吧"再建 team software-xxx 走 PM→架构→工程→QA。

## 功能：8 点混合增强（标准 SOP 全流程，版本号 v3.1.06）

**用户需求**（8 点，一次性全做）：见上「新需求批次」8 点清单。规格：图片≤10MB/视频≤100MB；复盘按周一起算周键；作品日期不限区间默认今天；密码成功闪示4门统一「✓ 验证通过」；中心提示统一语义色(绿/橙/红)。

**流程**：标准 SOP 全流程。建 team software-v3.1.06-batch → PM(PRD) → 架构(设计,落盘 docs/system_design.md+2张mermaid) → 工程(编码) → QA(验证)。

**改动文件**：
- 新增 `features/toast-center.js`：导出 `showCenterToast(type,text,duration)`，三态 ok/warn/err 居中浮层，色取 `--ok/--warn/--err`。
- `index.html`：`:root`+5主题定义 `--ok/--warn/--err`；`.cal-cell.has-pts .day-num` 加粗+主题色(③)；作品表单加 `#workDate`(⑦)；上传框 `<small>` 写明「图片≤10MB、视频≤100MB」+`accept`(⑧)；脚本 `main.js?v=3.1.06`。
- `features/render.js`：导入 showCenterToast；新增导出 `switchToCategory`(①)；补卡不足→`showCenterToast('warn',...)`(②:409)；兑换差额文案`还差 X 分`(⑤:724)；`renderReviewTimeline` 每条可编辑(去掉 isLatest 限制)(⑥)。
- `features/password.js`：成功分支 `showCenterToast('ok','✓ 验证通过')`(⑤:103)，覆盖4门；补 `dismissAutofill` export(顺带修潜在崩溃)。
- `core/helpers.js`：新增 `getWeekKey(ds)`(周一为起点)(⑥)。
- `core/data.js`：旧 reviews 加载时按 date 推导 weekKey 合并迁移(⑥)。
- `main.js`：导入 getWeekKey/switchToCategory/dismissAutofill；常量 MAX_IMG_MB=10/MAX_VIDEO_MB=100；doSaveAfterPassword 关弹窗+`switchToCategory('学习力')`(①)；saveReview 按 weekKey upsert(⑥)；作品表单 `#workDate`+`getWorkDate()`+`renderWorksDropdown` 跟随所选日期+`artwork.date=wd`(⑦)；saveWork `artwork.id` 去重+大小校验(⑧)；SW_VERSION/register→v3.1.06。
- `sw.js`：CACHE_NAME/ASSETS 9处→v3.1.06。
- `features/parent-center.js`：补 dismissAutofill import(顺带修潜在崩溃，逻辑未变)。

**验证**：QA 回归 PASS——13处版本号一致、8个JS node --check全过、15/15 逻辑仿真(getWeekKey周一/saveReview同周upsert/saveWork去重/作品date取所选日期)、T1-T9 静态确认全落地。路由 NoOne，无源码缺陷。

**未推送**（铁律）：所有改动留本地工作树，未提交未推 GitHub/Gitee。

**部署**：本轮用户未明确要求部署；UI强相关改动建议部署 CloudStudio 沙箱预览实测（沙箱≠GitHub/Gitee 推送）。待用户确认后部署。

## 部署：v3.1.06 CloudStudio 沙箱预览（用户确认「要」）

用户回复「要」即确认部署 v3.1.06 预览。

**操作**：抽干净运行时目录 `.deploy-staging`（仅 index.html/main.js/sw.js/manifest.json/icon-512.png/features/+/core/，1.3M，剔除 .git/node_modules/bundle.js 等），上传 CloudStudio 新沙箱。

**新沙箱链接**：https://5c08ed401ac4406e9952be0034dc8653.app.codebuddy.work

**线上验证（curl）**：index.html 引用 `main.js?v=3.1.06`；main.js 含 `v3.1.06`；关键新功能串 showCenterToast/还差/getWeekKey/switchToCategory 全部在线。版本号一致，v3.1.06 实锤。

**未推送**（铁律）：仅 CloudStudio 沙箱预览，未提交未推 GitHub/Gitee。本地工作树改动保留。
